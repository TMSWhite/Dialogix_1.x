README FILE for DIALOGIX

Run process_files.bat to copy files from where they are collected (/usr/local/dialogix), 
to the data directories, and run the Perl scripts on them.

You may need to change the JAR = line in _study.conf so that it refers to the actual location of JAR.exe